import styled from "styled-components";

export const LoadMoreBtnCss = styled.button`
  margin-left: 563px;
  margin-right: 680px;
  background-color: inherit;
  border: transparent;
  width: 79px;

  color: var(--primary-blue);
  font-family: var(--primary-font);
  font-size: 16px;
  font-weight: 500;
  line-height: 1.5;

  text-align: left;
  text-decoration-line: underline;
`;
